name: Release

on:
  release:
    types: [ created ]

jobs:
  ci:
    if: ${{ github.event.release.prerelease == true }}
    uses: ./.github/workflows/ci.yaml
    with:
      artifact-name: mdbook-site-release

  release-artifacts:
    if: ${{ github.event.release.prerelease == true }}
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get latest master commit short SHA
        id: sha
        run: echo "sha=$(git rev-parse --short origin/master)" >> $GITHUB_OUTPUT

      - name: Delete old release tag
        if: ${{ github.event.release.tag_name != '' }}
        run: |
          OLD_TAG=${{ github.event.release.tag_name }}
          echo "Deleting old tag: $OLD_TAG"
          git push origin --delete refs/tags/$OLD_TAG || echo "Old tag not found, skipping"

      - name: Update release tag to short SHA
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const releaseId = context.payload.release.id;
            const sha = "${{ steps.sha.outputs.sha }}";

            await github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: releaseId,
              tag_name: sha
            });

            core.info("Release tag updated to short SHA: " + sha);

      - name: Download built site
        uses: actions/download-artifact@v5
        with:
          name: mdbook-site-release
          path: book/

      - name: Package HTML artifacts as tar.gz
        run: |
          mkdir -p release-artifacts
          tar -czf release-artifacts/mdbook-html.tar.gz -C book .

      - name: Upload release artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-artifacts/mdbook-html.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
